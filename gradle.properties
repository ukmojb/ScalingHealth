# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs = -Xmx3G

# Mod Information
mod_version = 1.0
maven_group = net.silentchaos512
archives_base_name = ScalingHealth

# If any properties changes below this line, run `gradlew setupDecompWorkspace` and refresh gradle again to ensure everything is working correctly.

# Boilerplate Options
use_mixins = false
use_coremod = false
use_assetmover = false

# Access Transformer files should be in the root of `resources` folder and with the filename formatted as: `{archives_base_name}_at.cfg`
use_access_transformer = false

# Coremod Arguments
include_mod = true
coremod_plugin_class_name = 


group = net.silentchaos512
archivesBaseName = ScalingHealth
name = Scaling Health
description = Scaling Health

mc_version=1.12.2
sl_version=1.12-SNAPSHOT
jei_version=4.14.4.259
gamestages_version=2.0.91

fg_version = ForgeGradle:2.3-SNAPSHOT
forge_mc_version = 1.12.2
forge_version = 14.23.5.2838
mcp_mappings = stable_39

lombok_version = 1.18.0